---
import { cn } from "@/utils/cn";
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"ol">;

const { class: className, ...props } = Astro.props;
---

<ol class={cn("pl-6 space-y-2 list-decimal", className)} {...props}>
  <slot />
</ol>
